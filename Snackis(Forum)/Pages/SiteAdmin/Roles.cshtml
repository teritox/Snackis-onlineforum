@page
@model Snackis_Forum_.Pages.SiteAdmin.RolesModel
@{
}

<h1>
    Adminsidan - Du är
    @foreach (var role in Model.Roles)
    {
        <span>@(await Model._userManager.IsInRoleAsync(Model.CurrentUser, role.Name) ? role.Name + ", " : "")</span>
    }


</h1>
<p>Klicka här för att ändra sidans namn och kategorier</p>
<a class="btn btn-success" asp-page="/SiteAdmin/Index">Ändra innehåll</a><br />

<p>Klicka här för att hantera anmälda inlägg</p>
<a class="btn btn-danger" asp-page="/SiteAdmin/ReportedPosts">Anmälda inlägg</a><br />

<p>Klicka här för att se och lägga till fula ord</p>
<a class="btn btn-warning" asp-page="/SiteAdmin/BadWords">Fula ord</a><br />

@if (Model.isAdmin)
{
    <p>Det här får bara adminstratören se</p>
}
@if (Model.isUser)
{
    <p>Det här får bara användarna se</p>
}


@*<div>
        Befintliga roller: <br />
        @foreach (var role in Model.Roles)
        {
            <p>@role.Name</p>
        }

    </div>*@
<div>
    Befintliga användare: <br />

    <table>

        <tr>
            <th>Användarnamn</th>
            @foreach (var role in Model.Roles)
            {
                <th>@role.Name</th>
            }
        </tr>


        @foreach (var user in Model.Users)
        {
            <tr>
                <td>@user.UserName</td>

                @foreach (var role in Model.Roles)
                {
                    if (await Model._userManager.IsInRoleAsync(user, role.Name))
                    {
                        <td><a href="?RemoveUserId=@user.Id&Role=@role.Name" class="bg-success text-white">@role.Name</a></td>

                    }
                    else
                    {
                        <td><a href="?AddUserId=@user.Id&Role=@role.Name" class="bg-danger text-white">@role.Name</a></td>
                    }
                }

            </tr>
        }
    </table>

</div>


<form method="post">
    <label for="RoleName">Ny roll</label>
    <input name="RoleName" />
    <input type="submit" value="Spara Roll" />
</form>